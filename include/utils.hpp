#pragma once

#include <iostream>
#include <algorithm>
#include <vector>

//* Initialise and exit the program
void init_program();
void exit_program();

//* General helpers
std::string extend_string(std::string str, int times);
